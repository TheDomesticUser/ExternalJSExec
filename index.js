// const key = 'code';
// const url = document.URL;

// let code = decodeURI(url.slice(url.indexOf(key) + key.length + 1));

// // Get the code separated by the {} brackets
// code = code.split('{').pop().split('}')[0];

// eval(code);

alert(1);